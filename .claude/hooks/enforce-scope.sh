#!/usr/bin/env bash
#
# enforce-scope.sh (stub for Phase 6)
#
# Phase 6 deliverable. Will reject any file write performed during a
# `builder` subagent run when the target path falls outside the active
# spec's `scope_paths` glob. Fail loud with the path attempted and the
# active scope. No-op until Phase 6 wires the invocation.

set -uo pipefail
exit 0
