---
name: verify-contracts
description: Run the contract harness (fixtures, invariants, DOM or response contracts). Emit machine-readable results that the validator consumes. The validator reads this output, not the source code, so it cannot be fooled by code that looks right.
---

Stub. Phase 2 deliverable.
