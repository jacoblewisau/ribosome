# Spec 0009

Issue: #71 (ribo:tweak, doc-drift)
Researcher: chain 0009 light pass (single-file README.md copy fix)

## Path selection

`docs-only`. This is a documentation copy change to one sentence in `README.md`. No source, schema, workflow, or contract surface is touched. The substrate verifier matrix (`src/verify/`) is unaffected, so the short doc path is correct. One acceptance test guards the corrected wording.

## Data model changes

None.

## API or background flow

None.

## UI changes

None.

## The change

`README.md`, "Cost expectation" section. The sentence currently spanning lines 66 to 68 reads:

```
See `MEMORY.md` and the dream
skill for what the maintainer keeps; Opus is recommended for the dream
nightly cron during the first months of operation.
```

Replace the clause after the semicolon. The stale phrase "the dream nightly cron during the first months of operation" implies a scheduled workflow runs the dream consolidation pass. None exists (verified: the only dream-named workflow is `scout-dreamer-digest.yml`, which runs the weekly digest scout, not the consolidation pass; `.claude/skills/dream/SKILL.md` frontmatter confirms the pass "Runs manually for now").

Corrected wording for the full sentence:

```
See `MEMORY.md` and the dream
skill for what the maintainer keeps; the dream consolidation pass is run
manually by the maintainer, and Opus is recommended when running it.
```

Exact rules:
- Remove every occurrence of "nightly cron" from `README.md` (the only one is in this sentence).
- Do not invent a command name. The dream consolidation pass is the `dream` skill run in a Claude Code session; there is no `npm run dream` script, so do not reference one.
- Keep referring to "the dream skill" and "the dream consolidation pass"; do not rename either.
- Prose conventions: no en or em dashes, no emoji. Plain hyphens in compound words are fine.
- Touch only this sentence. Do NOT alter the separate stale phrase in `.claude/skills/dream/SKILL.md` (Issue #69, out of scope), and do not edit any other line of `README.md`.

## Tests required

### Acceptance test

`tests/acceptance/0009.spec.ts`. One file. Read `README.md` from the repo root (resolve the path relative to the test file or via `process.cwd()`), then:

1. Assert the file does NOT contain the substring `nightly cron` (the stale phrase is gone).
2. Assert the file DOES contain the substring `run\nmanually by the maintainer` is awkward across the wrapped line; instead assert it contains `run` and `manually by the maintainer` such that the corrected clause is present. Concretely: assert `README.md` contains the substring `manually by the maintainer` (proves the corrected clause landed) and still contains `the dream` (proves the dream skill reference is preserved).

Use the existing vitest + Node harness style. Reading a file is plain `node:fs` (`readFileSync`); follow `tests/acceptance/0007.spec.ts` for imports and the strict-TypeScript nullable conventions. No happy-dom render is needed since there is no component.

## Files to change

- `README.md` (modify): correct the one sentence per "The change".
- `tests/acceptance/0009.spec.ts` (add): the acceptance test above.
- `specs/0009.md` (add): this spec, committed with the change (staged by the builder).

## scope_paths

```
README.md
tests/acceptance/0009.spec.ts
```

## Risks

- Wording drift: mitigated by the exact corrected sentence above and the acceptance test asserting both removal of the stale phrase and presence of the corrected clause.
- Scope creep into Issue #69's separate stale phrase: mitigated by the explicit out-of-scope rule; the builder touches only the README sentence.

## Decisions captured

- **Assumed (inform-only):** corrected the clause to state the dream consolidation pass is run manually by the maintainer, matching the dream skill's own frontmatter. No new command or service is introduced.

## Open questions

None.
