# Spec 0008

Story: none (ribo:tweak fast-path, no story gate)

## Path selection

`docs-only`. This is a documentation tweak flagged by the doc-drift scout: it adds four `npm run` lines to one README section. No code, no data model, no UI, no contracts.

## Data model changes

None.

## API or background flow

None.

## UI changes

None.

## Tests required

None. The researcher confirmed no contract or acceptance test asserts on the README "Inspection commands" block. `src/state/build.test.ts` references README only via a dummy marker (does not assert content) and `src/evals/tasks.ts` asserts on `package.json`, not README. A docs-only tweak fast-path does not add an acceptance test.

## Files to change

- `README.md` (modify): in the "Inspection commands (maintainer only)" fenced code block (lines 159 to 165), add four lines for the maintainer scripts that exist in `package.json` but are currently undiscoverable from the README:
  - `npm run chain:board       # rebuild the Mission Control board on demand`
  - `npm run capture:evidence  # capture browser screenshots for the evidence pipeline`
  - `npm run dream:decay       # decay or purge stale distilled items`
  - `npm run eval              # run the Phase 5 structural eval harness`

  Match the existing aligned, lowercase, no-trailing-period comment style. Keep wording consistent with CLAUDE.md's Commands section.

## scope_paths

`["README.md"]`

## Risks

- Drift: README and CLAUDE.md list overlapping but non-identical command sets. Mitigation: phrase the new comments consistently with CLAUDE.md's Commands section (done above). No security, tenancy, timezone, idempotency, performance, or accessibility risk for a docs-only change.

## Decisions captured

- **Needs you (ask):** none.
- **Assumed (inform-only):** the four scripts named by the doc-drift scout are the complete set to add; `state:build` is intentionally not duplicated here since it is already documented in CLAUDE.md and is a build step rather than an inspection command.
- **ADR proposed:** none.

## Open questions

None.

## Tweak gate line

```json
{ "skill": "spec-writer", "mode": "tweak", "flags": [], "files": 1, "lines": 4 }
```
