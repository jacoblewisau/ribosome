### 2026-08-03 - Chain 0008 (tweak, #94): low-risk npm dependency floor bumps

Dep-scanner-filed `ribo:tweak`. Shipped only the low-risk patch/minor group; the
major bumps in the Issue were deferred (breaking-change risk, not tweak-sized).

- Raised six caret floors in `package.json` and regenerated `package-lock.json`:
  react and react-dom `^19.0.0` -> `^19.2.8`; `@types/react` -> `^19.2.18`,
  `@types/react-dom` -> `^19.2.4`, `playwright` -> `^1.62.1`,
  `@types/node` -> `^25.9.5`. All were already inside the existing caret ranges,
  so this pins the new minimums.
- Deferred (held out of this tweak, one-major-at-a-time work): `@types/node` 26,
  `@vitejs/plugin-react` 6, `happy-dom` 20, `typescript` 7, `vite` 8,
  `vitest` 4, `zod` 4. These cross a caret boundary and carry breaking-change
  risk across the typecheck/test/verify/build pipeline.
- No source or test changes. Regression gate stayed green: typecheck exit 0,
  112 unit tests, verify matrix 22 pass / 0 fail / 0 blocked (6 probes).
